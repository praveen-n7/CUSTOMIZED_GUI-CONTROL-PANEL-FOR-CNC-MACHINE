# Simple HAL configuration for QtVCP simulator
# LinuxCNC 2.9.8 compatible - minimal spindle config

loadrt [KINS]KINEMATICS
loadrt [EMCMOT]EMCMOT servo_period_nsec=[EMCMOT]SERVO_PERIOD num_joints=[KINS]JOINTS

# Add functions to servo thread
addf motion-command-handler servo-thread
addf motion-controller servo-thread

# Estop loopback
net estop-loop iocontrol.0.user-enable-out => iocontrol.0.emc-enable-in

# Tool change loopback
net tool-change iocontrol.0.tool-change => iocontrol.0.tool-changed
net tool-prep iocontrol.0.tool-prepare => iocontrol.0.tool-prepared

# Simulated machine - position command = position feedback
net j0pos joint.0.motor-pos-cmd => joint.0.motor-pos-fb
net j1pos joint.1.motor-pos-cmd => joint.1.motor-pos-fb
net j2pos joint.2.motor-pos-cmd => joint.2.motor-pos-fb

# Home switches - always on for simulator
setp joint.0.home-sw-in 1
setp joint.1.home-sw-in 1
setp joint.2.home-sw-in 1

# Spindle simulation - minimal configuration
# Speed feedback loop
net spindle-vel-cmd spindle.0.speed-out => spindle.0.speed-in

# Spindle on signal
net spindle-on spindle.0.on

# Spindle direction signals
net spindle-cw spindle.0.forward
net spindle-ccw spindle.0.reverse
